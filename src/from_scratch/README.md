# From Scratch

这里存放不依赖高级机器学习训练 API、使用 NumPy 手动实现的模型。

- `linear_regression_from_scratch.py`：MSE + Gradient Descent 的线性回归；
- `logistic_regression_from_scratch.py`：Sigmoid + BCE + Gradient Descent 的逻辑回归。

